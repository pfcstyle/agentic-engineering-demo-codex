{
  "name": "incident-triage-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
